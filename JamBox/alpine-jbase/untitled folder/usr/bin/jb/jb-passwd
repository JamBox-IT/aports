#!/bin/sh
#################################################
# TEST not PRODUCTION
# Needs error checking and return codes
#
# Corey DeLasaux <cordelster@gmail.com> (2011)
#################################################
USER=$1
passwd $USER
if [ -d /etc/acf ]; then
acfpasswd -s $USER
fi
exit 0