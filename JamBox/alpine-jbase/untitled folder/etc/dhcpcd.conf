# Inform the DHCP server of our hostname for DDNS.
hostname

# Use the hardware address of the interface for the Client ID.
clientid

# Rapid commit support.
# Safe to enable by default because it requires the equivalent option set
# on the server to actually work.
option rapid_commit

# A list of options to request from the DHCP server.
option domain_name_servers, domain_name, domain_search, host_name
option classless_static_routes
# Most distributions have NTP support.
option ntp_servers
# Respect the network MTU. This is applied to DHCP routes.
option interface_mtu

# A ServerID is required by RFC2131.
require dhcp_server_identifier

# Generate Stable Private IPv6 Addresses instead of hardware based ones.
slaac private

# Don't send any ARP requests.
noarp

# Only configure IPv4.
ipv4only

# Default Lease Time in seconds.
leasetime 600

# Wait 15 seconds before falling back to static profile.
reboot 15

denyinterfaces wlan0 phy0

# Configure loopback interface.
interface lo
static ip_address=127.0.0.1/8

### FUTURE USE ###
# Bring up Bridge br0
#interface br0
#bridge-ports uap0 usb0
#bridge-stp 1
#static ip_address=192.168.45.1/24

### Future add to bridge ###
# Static IP address for Wi-Fi access point.
interface uap0
static ip_address=192.168.45.1/24
nohook wpa_supplicant

### Future add to bridge ###
#static interface for USB Gadget
profile static_usb0
static ip_address=192.168.46.1/24

#interface for USB Gadget
# Start off DHCP incase internet sharing is enabled on workstation
interface usb0
fallback static_usb0

# Define static profiles.
profile static_eth0
static ip_address=192.168.1.100/24
static routers=192.168.1.1
static domain_name_servers=192.168.1.1

profile static_eth1
static ip_address=192.168.1.101/24

# Define wlan0 Wi-Fi station.
#interface wlan0

# Fallback to static profile on eth0.
interface eth0
fallback static_eth0

# Fallback to static profile on eth1.
interface eth1
fallback static_eth1
